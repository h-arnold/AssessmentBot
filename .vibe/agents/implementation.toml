name = "implementation"
description = "Implements code for the orchestrator"
sandbox_mode = "workspace-write"
auto_approve = true
safety = "destructive"
agent_type = "subagent"
nickname_candidates = ["implementation", "Implementation", "Impl"]
system_prompt_id = "implementation"

[tools]
allowlist = ["bash", "grep", "read_file", "search_replace", "todo", "write_file"]

[tools.bash]
permission = "always"

[tools.write_file]
permission = "always"

[tools.web_fetch]
permission = "always"
