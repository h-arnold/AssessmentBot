import { describe, it } from 'vitest';

describe('googleClassrooms.zod schemas', () => {
    it.todo('GoogleClassroomSchema accepts the backend classroom summary contract');
    it.todo(
        'GoogleClassroomsResponseSchema rejects malformed classroom payloads and strips extra contract fields from the frontend cache'
    );
});
