## Frontend E2E Tests Coverage + Report

Run: `npm --prefix src/frontend run test:e2e` completed on 2026-05-15.

---

### 1) Test Failures (12 total)

- **app.spec.ts**
  1. settings tabs switch visible panels in the browser
     - chromium – Test timeout 30s exceeded; locator.click: Target crashed while clicking "Backend settings" tab.
     - src/frontend/e2e-tests/app.spec.ts:213 `await backendSettingsTab.click();`
     - Status: flakey timing/element lifecycle issue; rerun recommended.

  2. theme toggle remains operable after collapsing and expanding nav
     - chromium – Test timeout 30s exceeded on wait for header banner locator.
     - src/frontend/e2e-tests/app.spec.ts:83 `page.getByRole('banner')` and line 353 assertion context.
     - Status: suspect fragile selectors or race; evaluate stability.

- **assignment-definition-wizard-section-4.spec.ts** 3) update flow: document change + successful re-parse refreshes task rows
  - chromium – Test timeout 30s exceeded on `page.click('text=Assignments')`
  - src/frontend/e2e-tests/assignment-definition-wizard-section-4.spec.ts:61
  - Recommendation: ensure page navigation is stable and the initial route is ready before interaction.
  4. create mode fails closed locally when required topic or year-group reference data cannot be trusted or loaded
     - chromium – page.waitForSelector: Target crashed; could not wait for "Assignment definitions" text.
     - src/frontend/e2e-tests/assignment-definition-wizard-section-4.spec.ts:160

- **classes-crud-manage-cohorts.spec.ts** 5) creates a new cohort and shows it in the list
  - chromium – expect modal text "Cohort 2026" to be visible failed; element not found within 5s.
  - src/frontend/e2e-tests/classes-crud-manage-cohorts.spec.ts:176

- **classes-crud-manage-year-groups.spec.ts** 6) creates a new year group and shows it in the list
  - chromium – expect modal text "Year 9" to be visible failed; element not found within 5s.
  - src/frontend/e2e-tests/classes-crud-manage-year-groups.spec.ts:154

- **select-with-add-new-workflow.spec.ts** (6 failures) 7) Cohort Select has Add new option – chromium timeout on clicking "create abclass" button.
  - src/frontend/e2e-tests/select-with-add-new-workflow.spec.ts:108
  8. Year Group Select has Add new option – same timeout on "create abclass" button.
     - src/frontend/e2e-tests/select-with-add-new-workflow.spec.ts:122
  9. Clicking Add new cohort opens Manage Cohorts modal – timeout on "create abclass" button.
     - src/frontend/e2e-tests/select-with-add-new-workflow.spec.ts:134
  10. Clicking Add new year group opens Manage Year Groups modal – same timeout.
      - src/frontend/e2e-tests/select-with-add-new-workflow.spec.ts:147
  11. Full workflow: Create cohort via Add new and auto-select – same timing issue.
      - src/frontend/e2e-tests/select-with-add-new-workflow.spec.ts:160
  12. Rapid clicks on Add new only open modal once (debounce) – same timeout on click.
      - src/frontend/e2e-tests/select-with-add-new-workflow.spec.ts:186

**Summary:** Most failures are timeouts and element-target crashes, concentrated in navigation/tab switching, modal assertion waits, and repeated "create abclass" button clicks. These are likely indicative of race conditions or unstable selectors rather than logic errors.

---

### 2) Miscellaneous Warnings / Observations

- **Playwright CLI:** npm warning about unknown config "workers"; actual flag not supported this way. Real concurrency is controlled by Playwright config rather than CLI.
- **Locale warning:** `/bin/bash: warning: setlocale: LC_ALL: cannot change locale (en_US.UTF-8)` – harmless in this environment; set a valid UTF-8 locale in CI if warnings are undesirable.
- **Test staleness/target crash messages:** Present in several logs (e.g., element interactions timing out, target crashed). Recommend stabilizing selectors and introducing explicit waits where needed.

---

### 3) E2E Coverage Status

- **No coverage report artifact was generated by Playwright** (`--coverage` is not a supported flag). Playwright itself does not produce coverage summaries; you would need to configure a coverage tool (e.g., c8) to gather coverage for Playwright runs.
- Currently there is no e2e-specific coverage gathering configured in `playwright.config.ts` or related configs.
- To collect code coverage for e2e tests:
  - Install c8: `npm install --save-dev c8`
  - Add script in Frontend package.json:
    ```json
    "test:e2e:coverage": "c8 npm --prefix src/frontend run test:e2e"
    ```
  - Configure c8 in `.c8rc.json` (or via package.json) to include/exclude files as needed.
  - Run: `npm run test:e2e:coverage`

---

### 4) Recommendations and Quick Wins

1. Stabilize selectors and waits around dynamic elements (tabs, modals, page navigation) to eliminate timeouts/target crashes.
2. Add explicit waits or state assertions before clicks/navigation to avoid race conditions when waiting for content.
3. Investigate and fix (or rerun to confirm consistency) the 12 failures above — likely flakey tests due to timing.
4. Introduce e2e code coverage with c8 as described above; add a new script `test:e2e:coverage` to the Frontend package.json.
5. Add CI steps that rerun failed tests (shard or `--repeat-each`) to gate flaky tests.

---

**Report generated:** 2026-05-15T19:04:00Z
