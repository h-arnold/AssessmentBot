# Builder Agent Instructions Moved

This documentation path is kept as a compatibility notice.

Builder agent instructions now live with the builder component at:

- `scripts/builder/AGENTS.md`

For cross-component routing and shared rules, read:

- `AGENTS.md` (repository root)

When working on `scripts/builder/**` or `build/**` pipeline behaviour, follow `scripts/builder/AGENTS.md` as the authoritative source.
