# Frontend Agent Instructions Moved

This documentation path is kept as a compatibility notice.

Frontend agent instructions now live with the frontend component at:

- `src/frontend/AGENTS.md`

For cross-component routing and shared rules, read:

- `AGENTS.md` (repository root)

When working on `src/frontend/**`, follow `src/frontend/AGENTS.md` as the authoritative source.
