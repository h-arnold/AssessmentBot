# Backend Agent Instructions Moved

This documentation path is kept as a compatibility notice.

Backend agent instructions now live with the backend component at:

- `src/backend/AGENTS.md`

For cross-component routing and shared rules, read:

- `AGENTS.md` (repository root)

When working on `src/backend/**`, follow `src/backend/AGENTS.md` as the authoritative source.
